package com.bitbar.remotedevice.websocket.listeners;

import com.testdroid.api.model.APIConnection;

public interface APIConnectionListener {

    void handleAPIConnection(APIConnection connection);

}
